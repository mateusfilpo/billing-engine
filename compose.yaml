services:
  postgres:
    image: 'postgres:18'
    environment:
      - 'POSTGRES_DB=billing_db'
      - 'POSTGRES_USER=admin'
      - 'POSTGRES_PASSWORD=admin'
    ports:
      - '5432:5432'
  
  adminer:
    image: adminer
    restart: always
    ports:
      - '8081:8080'
